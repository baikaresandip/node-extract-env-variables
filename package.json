{
  "name": "extract-env",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sandip baikare",
  "license": "ISC",
  "description": "This repo will extract the environment variables in the .env.example file of the repo.",
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
